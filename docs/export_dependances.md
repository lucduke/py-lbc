# Exporter des dépendances

## Commande pip
Pour exporter les dépendances de votre projet Python dans un fichier `requirements.txt`, vous pouvez utiliser la commande suivante :

```bash
pip freeze > requirements.txt
```
